{
  "name": "piter",
  "version": "0.1.2",
  "description": "paginated (and optionally parallel) iterator over anything",
  "main": "lib/index.js",
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "format": "prettier --write '{README.md,src/**/*.js}'",
    "prepublish": "yarn build"
  },
  "keywords": [
    "async-iterator",
    "iterator",
    "cursor",
    "paginate"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^25.1.3",
    "jest": "^25.1.0",
    "prettier": "1.19.1",
    "ts-jest": "^25.2.1",
    "typescript": "^3.8.2"
  },
  "dependencies": {
    "@types/node": "^13.7.6"
  }
}
